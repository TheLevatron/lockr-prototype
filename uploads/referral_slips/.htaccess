# Prevent direct access to uploaded files
# Only allow access through application logic

<FilesMatch ".*">
    Order Deny,Allow
    Deny from all
</FilesMatch>

# Allow images to be displayed when accessed through the application
<FilesMatch "\.(jpg|jpeg|png|gif)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>
